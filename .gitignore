.env
__pycache__/
*.pyc
*.db
*.log
.DS_Store
venv/
.venv/
.trader.pid
.dashboard.pid
.env.compose

# Phase B — additional runtime artifacts
trades.db-wal
trades.db-shm
*.log.*

# LLM call logs (potential training data — kept local, not committed)
logs/
.last_cycle.txt

# Tunable parameter runtime state — defaults live in utils/tunable_params.py:_SPECS
tunable_params.json

# Bot runtime state (paused_portfolios, last_scan_date) — written every cycle
.state.json

# Generated digests (premarket/weekend briefs) — runtime output, written per cycle
digests/

# Weekend-brief runtime state — written per weekend sweep
data/last_weekend_brief.json

# Migration runtime (account split) — local only, never committed
.migration_keys.env
.migration_done
